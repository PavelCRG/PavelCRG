# assets/

Статичные SVG для оформления профиля. Пара **dark** / **\*-light.svg** — README подключает одну картинку через `<picture>` (без дублей).

Карточки **stats / streak / languages** — в `stats-output/` и `languages-output/`, обновляются Actions из публичных репозиториев GitHub.
